This folder will contain:
- best.pth    → Best model weights (created after training)
- last.pth    → Last training checkpoint
- classes.json → Class names mapping

Run `python train.py` to generate these files.
